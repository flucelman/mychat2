app:
  name: "backend"
  port: "8080"

database:
  host: "localhost"
  port: "3306"
  username: "root"
  password: ""  #写到了.env文件中
  name: "mychat"

redis:
  host: "localhost"
  port: "6379"
  password: "" #写到了.env文件中
  db: 0

